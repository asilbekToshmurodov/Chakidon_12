# Chakidon_12

The first bot deployment with AI and with no knowledge at all.

## Usage

Run the helper script with a task description:

```bash
./cleaning_bot "Sweep the lobby and wipe counters"
```

Or run the Python module directly:

```bash
python3 cleaning_bot.py "Empty bins and restock supplies"
```
